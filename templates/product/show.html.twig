{% extends 'base.html.twig' %}

{% block title %}Ceramic{% endblock %}

{% block body %}
    <h1>Ceramic</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ product.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ product.description }}</td>
            </tr>
        </tbody>
    </table>

   {% for image2 in product.image2s %}
        <img src="{{ asset(image2.path) }}" alt="" width = 90 height = 90>
   {%  endfor %}

    <a href="{{ path('product_index') }}">back to list</a>

    <a href="{{ path('product_edit', {'id': product.id}) }}">edit</a>

    {{ include('product/_delete_form.html.twig') }}
{% endblock %}


